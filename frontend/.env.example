# Leave empty for local development (Vite proxy handles it)
# For production, set to your Render backend URL (no trailing slash)
# Example: VITE_API_URL=https://mediq-backend.onrender.com
VITE_API_URL=
